# Claude Adapter

Read `AGENTS.md` in full before taking any action. It is the canonical workspace
protocol for scope, active v2 context, orchestration, external memory, verification,
sensitive data, Git, Linear, and the holdout boundary.

Do not duplicate or override that protocol here. When `AGENTS.md` changes, this
adapter should remain a pointer so Claude and other agent runtimes follow the same
harness.
